class StoreProcedurePointInformationDto
{
    constructor(all, current, status){
        this.all = all
        this.current = current
        this.status = status
    }
}

export default StoreProcedurePointInformationDto